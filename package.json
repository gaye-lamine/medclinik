{
  "name": "medclinik",
  "version": "1.0.0",
  "description": "MedClinik ERP - Multi-role Medical ERP (NestJS + Next.js + PostgreSQL)",
  "private": true,
  "scripts": {
    "db:up": "docker compose up -d",
    "db:seed": "npm run db:seed --prefix backend",
    "start:backend": "npm run start:dev --prefix backend",
    "start:frontend": "npm run dev --prefix frontend -- -p 3005",
    "start": "npx -y concurrently \"npm run start:backend\" \"npm run start:frontend\""
  }
}
